{
  "scripts": {
    "build": "cd frontend && npm install && npm run build && rm -rf ../backend/public && cp -r dist ../backend/public && cd ../backend && npm install",
    "start": "cd backend && npm run start"
  }
}
